import SwiftUI

extension Color {
    /// Ibis's hero color — kelly green. Used sparingly, for accents and the
    /// "pop of color" the app leans on. Also mirrored in the `AccentColor`
    /// asset once the asset-catalog build setting is wired up.
    static let ibisKelly = Color(.sRGB, red: 0.298, green: 0.733, blue: 0.090, opacity: 1)
}
